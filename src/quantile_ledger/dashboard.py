"""Local Streamlit dashboard entry (Milestone 8).

This module intentionally performs no network I/O and does not train models.
"""

from __future__ import annotations


def main() -> None:
    message = (
        "QuantileLedger dashboard is not implemented until Milestone 8.\n"
        "All panels will read the local SQLite database only.\n"
        "Paper-only: this application never places real orders."
    )
    raise SystemExit(message)


if __name__ == "__main__":
    main()
